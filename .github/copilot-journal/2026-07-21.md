## 2026-07-21

Completed public-release prep: added Apache-2.0 LICENSE, rewrote stale generator/README.md (was a copy of the old openapi_specs/ tool), updated root README with prerequisites and public-source-only provenance note. Bumped go.mod to 1.24 (required for `tool` directive), aligned both workflow files. Ran act locally — test, build, smoke-merge all passed. gitleaks scan: 433 findings, all false positives (placeholder keys/PEM examples). Fixed okta_token_service README private GitHub link. GRS removal (plan step 2) still outstanding.

---

### Round 2

Deprecated `rightscale-global-resource-service` (empty paths/components, private repo source) and renamed `flexera-grs-v2` → `flexera-grs` in specs.yaml. Deleted `sources/rightscale/global_resource_service/` directory. Scrubbed private `github.com/flexera/iam` reference from GRS spec description and specs.yaml. Fixed corrupted test.yml (Docker paste injected mid-step). Build+tests pass locally. act smoke-test passes; act build step shows a known git-ref error that resolves once the initial commit exists.
